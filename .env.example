# Data Source Configuration
# Choose between 'json' (default) or 'cms' as the data source
# json: Loads data from local JSON files (src/data/schedule2025.json)
# cms: Loads data from a CMS API endpoint (requires VITE_CMS_API_URL)
VITE_DATA_SOURCE=json

# CMS API URL (only required if VITE_DATA_SOURCE=cms)
# VITE_CMS_API_URL=https://your-cms-api.com/api
